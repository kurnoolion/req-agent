# Telecom-requirement variant of SIRA's query_v07.txt.
# Mirrors the doc_requirement_v01.txt vocabulary universe.
You are expanding a search query against a corpus of US Mobile Network Operator (MNO) device requirement specifications. Generate NEW terms that are NOT already present in the query.

Query:
{doc_text}

Your task: generate vocabulary that the relevant device-requirement document would use, but that is MISSING from the query.

CRITICAL RULES:
- Do NOT guess the answer to the question. If the query asks "what value does timer T3402 use", do NOT generate specific timer values. If it asks "which procedure handles X", do NOT generate procedure-name guesses. If it asks "which plan defines Y", do NOT generate plan codes you're guessing. The answer is UNKNOWN — your job is to help FIND it, not to predict it.
- Instead, expand with TOPIC and DOMAIN vocabulary: the relevant 3GPP / GSMA spec family, the procedure family, the network-element category, and contextual terms that the answering requirement would use alongside the answer.
- For questions about procedures (attach, registration, handover, paging): generate procedure-family names ("EPS Mobility Management", "NAS layer", "RRC Connection"), spec family ("3GPP TS 24.301", "TS 23.401") — NOT specific cause codes, specific timer values, or specific message-IE values that you'd be guessing.
- For questions about timers / counters: generate timer-family names ("T3xxx timer family", "EMM timer", "NAS retransmission"), behavior categories ("exponential backoff", "PLMN-specific timer") — NOT specific durations or specific transition rules you'd be guessing.
- For questions about features / applicability: generate feature-family names ("VoLTE registration", "IMS subscription", "data retry") and applicability dimensions ("form factor", "service type", "RAT") — NOT specific yes/no answers.

Good example: "What is the T3402 timer behavior in VZW data retry?" → expand with: EMM timer, mobility management retry, PLMN throttling, 3GPP TS 24.301 Section 5, exponential backoff (NOT specific seconds, NOT specific PLMN names)
Bad example: same query → 12 minutes, Verizon, AT&T, succeeded (guessing values or wrong scope)

Rules:
- Do NOT include any word or phrase already present in the query text above.
- Do NOT generate specific req_ids — those aren't search terms.
- Generate at most 10 phrases, 1-{max_n} words each.
- Each phrase must add new vocabulary not already in the query.
- Be DIVERSE: cover multiple distinct facets of the topic — do not generate synonyms for the same concept.
- Prefer terms that NARROW the topic (e.g., "EPS Mobility Management" not "telecom"; "T3xxx timer" not "timer") without guessing the specific answer.

Think shortly, not more than 10 sentences. Then output only:
{{"keywords": ["...", "..."]}}
