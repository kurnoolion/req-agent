# Status

**Active phase**: development
**Last updated**: 2026-05-05
**Last drift-check**: 2026-05-05 — mode: dev-full — 3 drift(s) resolved (all direction [a]: MODULE.md → match code; resolver public surface for resolve_review + resolve_review_cli; web routers list + Depends on for new parse_review / req_browser / resolve_review routes), 3 deferred surfaced

## Done

- 2026-04-21 COMPACT scaffold retrofitted into existing codebase (project-init --retrofit); 16 Python packages seeded with MODULE.md skeletons; phase prompts generated.
- 2026-04-21 Curated all 16 MODULE.md skeletons (retrofit sentinels removed; commit 8395628).
- 2026-04-23 Flattened `.claude/skills/` layout so COMPACT slash commands are discoverable by Claude Code; user-global bundle source preserved at `~/.claude/compact-src/`.
- 2026-04-23 First `/drift-check dev-full` — 3 drifts deferred (2 in web, 1 in pipeline); Depends-on semantics clarified in `structure-conventions.md` to treat artifact coupling as a valid edge.
- 2026-04-27 Switched to requirements phase. Curated PROJECT.md (5-column Contributors with Validation channel, success criteria with ≥90% weighted-overall accuracy bar, scope clarified to single-MNO v1 + multi-MNO-ready schema, production reverse-proxy constraint added). Wrote `requirements.md` with 24 active FRs + 16 NFRs + 6 Deferred (compliance ×4 + test-case parser + DOC/XLS legacy extraction). Triaged all 7 Open questions to closure.
- 2026-04-27 Captured architectural reorg plan: FR-28..FR-30 added to `requirements.md` for `env_dir` / CLI parameterization; D-019..D-024 queued for next architecture-phase session (three-tier `core/` + `customizations/` + `config/` repo layout; per-env runtime directory containing `input/`, `out/`, `state/`, `corrections/`, `reports/`, `eval/`).
- 2026-04-27 Switched to architecture phase. Appended D-019..D-024 to `DECISIONS.md` covering three-tier code organization (`core/` + `customizations/` + `config/`), bi-directional `core ↔ customizations` dependency, one config file per module, per-env runtime directory `<env_dir>`, source layout `<env_dir>/input/<MNO>/<release>/`, and `customizations/` seeding (profiles + proprietary-LLM boilerplate). Rewrote `structure-conventions.md` with target layout + transitional banner. Updated CLAUDE.md Repository layout to flag the reorg in flight.
- 2026-04-27 `/drift-check design` batch — 9 R-vs-D drifts resolved (all direction [a]: MODULE.md → match requirements + DECISIONS). All 16 MODULE.md updated with FR/NFR citations and D-019..D-024 links; path conventions renamed `<doc_root>` → `<env_dir>` per D-022; extraction MODULE.md now commits to XLSX per FR-1.
- 2026-04-27 Slice A three-tier reorg executed (D-019..D-024) — 5 stages: A.1 file moves (src→core/src, tests→core/tests, profiles→customizations/profiles, web/config.json→config/web.json) + customizations/llm/ seed (`ProprietaryLLMProvider` stub per D-024 option ii); A.2 import rewrite (279 imports across 75 .py files); A.3 CLI rewrite (164 invocations across 21 docs/scripts); A.4 web config path resolution (PROJECT_ROOT depth fix + `config/web.json`); A.5 CLAUDE.md banner removed + MAP.md regenerated for new tree. 375 tests pass.
- 2026-04-27 Slice B executed (`<env_dir>` parameterization, FR-28..FR-30, D-022, D-023): `EnvironmentConfig` field/property/method renames (`document_root` → `env_dir`, aggressive per-partition methods); `PipelineContext.from_env` / `.standalone` threaded through `<env_dir>` / `out/` / `state/` / `corrections/` / `reports/` / `eval/`; `--env-dir` CLI flag; web runtime DBs under `<env_dir>/state/` (`WebConfig.env_dir` + `state_path()` / `jobs_db_path()` / `metrics_db_path()` helpers); extraction `infer_metadata_from_path` adapted for `<env_dir>/input/<MNO>/<release>/`; doc_type dropped from path inference per Q4 (defaults to "requirement"). 375 tests pass.
- 2026-04-27 Slice C executed (FR-1 XLSXExtractor): per-sheet heading + table-block extraction via openpyxl; registered as `.xlsx` extractor; 9 new tests with `pytest.importorskip("openpyxl")` gating. PDF extractor lazy-imports `fitz` / `pdfplumber` so the registry module loads on environments without pymupdf (matches stages.py pattern). Full suite at 384 passed, 2 skipped, 0 failed.
- 2026-04-27 `/drift-check dev-full` — 4 R/D-vs-I drifts resolved (all batch [a]: MODULE.md aligned to slice-B/C method renames). env Public surface + Invariant `correction_path` → `correction_file`; web Public surface `WebConfig.db_path` → `env_dir` + helpers; extraction Invariant drops PIL. Reorg fully landed and verified across all three layers.
- 2026-04-28 profile_debug `--create` mode for Ollama-driven profile bootstrap from extracted docs (commit 9d3b845); hardened against small-model prose preambles via system prompt + trailing "JSON only, start with `{`" reinforcement + tolerant `_extract_json_object` first-balanced-{...} extraction (0966b9d).
- 2026-04-28 Standards: pluggable `SpecDownloader.source` with HuggingFace as default (DOCX-only via stdlib urllib, no auth, no LibreOffice). 3GPP FTP path retained as fallback. New `core/src/standards/hf_source.py`; CLI `--standards-source`, `NORA_STANDARDS_SOURCE` env var, `EnvironmentConfig.standards_source` field; precedence resolver in `env.config.resolve_standards_source`. 13 new tests with mocked urllib. Commit 3c63b01.
- 2026-04-28 LLM: `OpenAICompatibleProvider` for cloud APIs (OpenRouter / Together / DeepInfra / Groq / OpenAI) — stdlib urllib, no SDK dependency. New `core/src/llm/openai_provider.py`; CLI `--llm-provider`, `NORA_LLM_PROVIDER` / `NORA_LLM_BASE_URL` / `NORA_LLM_API_KEY` / `NORA_LLM_MODEL` env vars; `EnvironmentConfig.model_provider` validation extended. 16 new tests. Commit d7e9aab.
- 2026-04-28 Pipeline extract stats fix: `ir.blocks`/`b.block_type` were wrong attribute names — every doc threw AttributeError that the surrounding except caught and re-emitted as EXT-E001, making it look like all PDFs failed extraction. Real impact: every prior compact report reported `tbl=0` while IRs in fact contained 1272 tables across the 5 OA PDFs. Commit 53e453f.
- 2026-04-28 Parser: dual-anchor — Requirements now created from table cells too (column-1 of row first, all-cells fallback; one anchor per row max; paragraph wins on duplicate `req_id`). New `_extract_table_anchored_reqs` + `_create_table_anchored_req` + `_propagate_hierarchy_to_table_reqs`; `_build_sections` tracks `paragraph_req_ids` for dedup. MODULE.md invariants + key choices updated to document the dual-anchor contract and `section_number=""` for table-anchored reqs. 10 new unit tests with hand-crafted in-memory fixtures + 1 existing test updated. **Suite: 426 passed / 52 skipped, 0 failed.** Commit c914ad0.
- 2026-04-28 First end-to-end OA-corpus pipeline runs on dev PC (Core Ultra 9, 15GB RAM, no GPU) via OpenRouter / Qwen3-235B-A22B. Three runs measured: A (pre-parser-fix) → 86.2% overall / 60.2% acc / 100% citation; A2 (post-parser-fix, mock fallback — invalid, env vars missing); A3 (post-parser-fix, real LLM) → 81.7% / 54.6% / 94.4%. Parser fix moved structural numbers up (req 711→985, unresolved internal refs 138→104, graph edges +21%, taxonomy features 28→35) but eval accuracy down 5.6 pts — diagnosed as retrieval pollution: 274 new table-anchored thin chunks distract the 18-Q eval set authored against the old paragraph-only corpus. Resolution pending per-question analysis. (Closes the "first end-to-end pipeline run" Next item from 2026-04-27 — done on dev PC rather than work laptop.)
- 2026-04-29 Vectorstore Ollama embedding backend (alternative to sentence-transformers / HF cache): new `OllamaEmbedder` (POST `/api/embeddings`, pings `/api/tags` on init, bypasses HTTP_PROXY for loopback hosts, L2-normalizes by default). New `make_embedder(config)` factory in `vectorstore/__init__.py`; `run_vectorstore` / `run_eval` migrated to it. 18 new tests. Commit 5e660f9.
- 2026-04-29 Pipeline embedding CLI parity with `--llm-provider`: `EnvironmentConfig.embedding_provider` + `embedding_model` fields with `resolve_embedding_provider` / `resolve_embedding_model` precedence (CLI > `NORA_EMBEDDING_{PROVIDER,MODEL}` env var > config > default). `PipelineContext` carries the fields; `run_cli` exposes `--embedding-provider {sentence-transformers,huggingface,ollama}` + `--embedding-model`. `make_embedder` accepts `huggingface`/`hf` aliases. `vectorstore_cli` migrated to the factory + refreshed `--provider` help (was stale). 8 new tests. Suite: 455 passed, 52 skipped. Commit fbe1856. Unblocks personal-PC (OpenRouter LLM + local HF embeddings) vs work-PC (local Ollama for both) two-machine workflow.
- 2026-05-01 Vectorstore + Ollama embedding hardening (5 commits): fixed `OllamaEmbedder.dimension` probe (was sending empty string; Qwen3 rejects — commit 3595978); included text length + preview in error messages (84b807b); added standalone `embed_debug` tool with --check / --text / --length-sweep / --chunks modes (31b1c26); capped per-text input at 8000 chars to dodge Ollama 500s on oversize chunks (5d3d1b0); tightened DECISIONS.md so it stays cheap to keep in context (ad07928). Ollama embeddings now work end-to-end on the work-laptop with `qwen3-embedding-q8-0:4b` against the OA corpus.
- 2026-05-01 Parser+profiler: numbering-driven heading classification, style as advisory hint only (commit 9df8a19). `_classify_heading` no longer requires font/style match — numbering pattern is the necessary signal; section_number deduplication added in `_build_sections`. Numbering pattern relaxed to match top-level chapters without trailing dots (`"2 LTE Data Retry"`). Profile method `numbering` emits one advisory level rule instead of the noisy per-cluster set. 7 new heading-classification tests + 1 profiler integration test. Suite: 465 passed, 52 skipped.
- 2026-05-01 Five new ingestion FRs (FR-31..FR-35) from real-corpus profile review against VZW OA: priority markers, form-factor applicability with hierarchical inheritance, strikeout content omission, TOC omission, definitions/acronyms expansion. Three architecture decisions captured (D-030 applicability, D-031 strikeout, D-032 definitions/acronyms). Five MODULE.md files curated (models, extraction, profiler, parser, vectorstore) with FR/D-XXX citations and additive Invariants / Key choices. Commit b5b73b5.
- 2026-05-01 Batch A (FR-34 TOC omission) implemented (commit 4eff40f). Profile gains `toc_detection_pattern` (default leader-dot-page-number regex) + `toc_page_threshold` (default 0.8). Parser drops matching blocks before heading classification, plus whole-page drop when threshold met. New `ParseStats` dataclass on `RequirementTree` (also reserves `struck_blocks_dropped` and `defs_extracted` for upcoming batches). Compact RPT parse line gains `toc=N` token. 4 new tests. Suite: 469 passed.
- 2026-05-01 Batch B (FR-31 priority marker) implemented (commit 48e2f47). Profile gains optional `HeadingDetection.priority_marker_pattern` (regex; capture group 1 = priority value; empty disables). Parser gains `Requirement.priority` attribute, populated via new `_extract_priority` helper invoked from `_build_sections` after section creation. Priority values uppercased on extraction so `mandatory` / `Mandatory` / `MANDATORY` collapse. 4 new tests. Suite: 473 passed. Verizon OA leaves the field empty (no priority convention in that corpus); other carriers can enable via the corrections workflow without code changes.
- 2026-05-01 Batches C / D / E completed: FR-33 strikeout (commit 5c03bd7 — IR `FontInfo.strikethrough` field, all 3 extractors PDF/DOCX/XLSX, parser drop, RPT `struck=N` token, 3 tests); FR-32 applicability inheritance (commit 3552c42 — `ApplicabilityDetection` schema, parser post-pass, `Requirement.applicability` attribute, 5 tests); FR-35 definitions/acronyms expansion (commit 0ac6d9f — `RequirementTree.definitions_map`, parser `_extract_definitions` post-pass, chunk_builder inline expansion with self-skip + word-boundary + idempotence, 14 tests). All 5 batches A–E now shipped.
- 2026-05-01 Real-corpus parser hardening from user review of `~/work/env_vzw/out/profile/profile.json` against the 5 OA PDFs (4 commits surfacing 7 distinct corpus-correctness bugs):
  - **commit d43f7a7** — three fixes: numbering pattern accepts `<digits><uppercase>` no-space variant (recovers 364 sub-sections lost when PDF extraction drops the space between section number and bold-rendered title); TOC page threshold lowered 0.8 → 0.7 (catches boundary pages where TOC entries wrap across blocks); req-id NO-LATERAL on extra small-font blocks (fixes systematic off-by-one cascade — OA req_ids are TRAILING markers, not leading).
  - **commit 68a1a55** — deferred two-pass table-anchored extraction + struck_req_ids tracking. Eliminated 257 phantom duplicates where a req's paragraph anchor on page N got duplicated as table-anchored from a cross-reference table on an earlier page.
  - **commit 8844a8e** — heading-continuation defense. PyMuPDF wraps long headings across blocks; when continuation line happens to start with `<digits><space><uppercase>` it was misclassified as a phantom depth-1 chapter. Eliminated 4 confirmed false positives (`_LTEB13NAC_1876` "13", `_LTESMS_30299` "404", + 2 auto-detected).
  - **commit 0e28e02** — req_id whitespace canonicalization. PDF extraction occasionally fuses bold runs and drops the underscore in `VZ_REQ_PLAN_NUM` → arrives as `VZ_REQ_PLAN NUM`. Profile pattern accepts either separator; parser canonicalizes to underscore. Recovered chapter-level `_LTEOTADM_65` and `_LTESMS_70` that were silently dropped.
- 2026-05-01 Audit tooling + integration safety net + remaining bugs (4 commits):
  - **commit 38ef63c** — OA-corpus integration test (`test_integration_oa_corpus.py`): 17 user-verified `req_id → section` ground-truth pairs + 2 struck-absence pairs + 5 macro-stat assertions. Module-scoped fixture: extract+profile+parse runs once per session.
  - **commit 29d96a4** — `parse_audit.py` per-document confidence scorer + correction template. Emits CSV with HIGH/MEDIUM/LOW classification + blank `correct_section` column. Empirical OA grand totals: HIGH=96% / MED=3% / LOW=0.1%. The 5 LOW rows surfaced exactly the bug list (4 false positives + 1 source-defect runaway).
  - **commit d30d6c4** — `enable_table_anchored_extraction` profile flag (default True for D-027 back-compat; flip via corrections to disable for paragraph-only-requirement corpora). With OA corrections=False: 33 table-anchored phantoms eliminated.
  - **commit fcf336f** — geometric strike detection on PDF table cells (FR-33 [D-031] completion). PDF strikethrough is a graphic operation (line drawn over text); pdfplumber doesn't surface it on cells. New `PDFExtractor._table_is_struck` counts ≥2 horizontal strike lines crossing ≥50% of table width within its vertical extent. Struck-block drop count jumped 379 → 1006 across the corpus. All 6 user-confirmed struck-table cases now correctly absent.
  - Cumulative: 11 corpus-correctness bugs fixed, 83 new tests, 0 regressions. Suite at 556 passed / 52 skipped (was 473 at session start). All 17 ground-truth pairs verified.
- 2026-05-02 Strike-handling completion (3 commits surfacing 4 layered fixes):
  - **commit 97537ee** — extraction: drop spurious 1×1 pdfplumber tables (paragraph-fragment hallucinations) + relax `_table_is_struck` to `min_lines=1` for 1-row tables. 510 phantom 1×1 tables eliminated; revhist headings/anchors collapse from 4× to 2× per doc.
  - **commit 425f8e3** — parser+profiler: profile-driven revision-history table omission (FR-34). New `revision_history_heading_pattern` field; profiler narrows the regex to corpus-observed phrasing (whitespace-tolerant); parser drops heading + window-bounded next-block table. 9 new tests.
  - **commit 7674719** — extraction: row-edge filter on `_table_is_struck`. Geometric probe revealed 93% of OA tables (709/762) were falsely flagged struck — pdfplumber row-divider lines were indistinguishable from strike marks. Filter: exclude strike candidates whose y aligns with any `Table.rows[*].bbox` edge (within 1.5pt). 4 new tests.
- 2026-05-02 Strike + cascade + revhist comprehensive (commit dfdc7e8). User feedback flagged 5 "preserved" tables that should still be dropped via two distinct mechanisms: (A) cell-level row-strike for partial-row deletions; (B) section-heading cascade for struck parents. Plus (C) extending revhist to consume multi-page table continuations. Implementation: `PDFExtractor._detect_struck_rows` (drops rows with mid-row strike lines from IR; if all rows drop, whole table marked struck); parser cascade state (`cascade_depth` armed by struck heading, drops subsequent blocks until next heading at depth ≤ cascade_depth — uses `_classify_heading` for boundary check so cascade-vs-non-heading-text discriminator matches the rest of the parser); revhist consume now drops table/image until next paragraph. New `ParseStats.cascade_blocks_dropped` counter. 13 new unit tests.
- 2026-05-02 Parser: table-anchored definitions extraction (FR-35 [D-032]) — commit 5043f7e. `defs=0` on the OA corpus traced to OA convention: glossary section has thin intro paragraph + 2-col tables (`Acronym/Term | Definition`). Extended `_extract_definitions` to scan both layouts; col[0]=term, col[1]=expansion with whitespace collapsed. defs=0 → 158 across 5 docs (LTEAT 26, LTEB13NAC 63, LTEDATARETRY 36, LTEOTADM 18, LTESMS 15). 3 new tests.
- 2026-05-02 Standards: STD-E002 nullguard (commit e65cca8). `SpecParser._parse_sections` accessed `p.style.name.startswith(...)` without checking `p.style is None`. Two specs (TS 22.220 Rel-9, TS 36.133 Rel-13) carried paragraphs without a `pStyle` element — valid OOXML — that crashed the parser. Fix: `getattr(p.style, "name", "") or ""`. Standards extracted: 52 → 54. 1 mock-based regression test.
- 2026-05-02 A4 eval baseline established (qwen3-235b-a22b via OpenRouter): **88.0% overall / 67.6% accuracy** (vs A3 81.7% / 54.6% with gemma4:e4b — +13.0pp accuracy, +6.3pp overall). Driven by 5 fixes:
  - `corrections/profile.json` numbering pattern relaxed: `^(\d+(?:\.\d+)*)\s+\S` → `^(?:(\d+)(?=\s)|(\d+(?:\.\d+)+)(?=\s|[A-Z]))` — accepts `1.4.3.1.1.9Upon receipt...` (no space) which OA generates often. Recovered 321 missing requirements (req 636 → 957, +50%); vectorstore chunks 547 → 794.
  - **commit 066c419** — query: entity-priority scoping in `graph_scope`. Specific-entity queries ("What is VZ_REQ_X?") were diluted by feature lookup (DATA_RETRY → 700 reqs) into a 794-candidate scope; vector ranking missed the named req. Fix: when `_entity_lookup` matches, skip `_feature_lookup`/`_plan_lookup`/`_title_search` — let edge traversal provide neighborhood context. trace_01 0% → 100%; traceability 16.7% → 50%.
  - **commit 158b7bb** — query: cross-doc list-style detection + type-aware `top_k`. List-style queries ("What are all the X requirements?") were classified `SINGLE_DOC` and used `top_k=10`, where the expected parent/overview reqs ranked at #15+ (chunks are short — heading + path only — vs leaf chunks with body content). Two-part fix: (i) analyzer adds list/breadth phrase triggers (`across all`, `what are all`, `all the requirements`, `across vzw specs`) → `CROSS_DOC`; (ii) `QueryPipeline` picks `top_k` from `_TYPE_TOP_K` map (CROSS_DOC/FEATURE_LEVEL/STANDARDS_COMPARISON → 25; TRACEABILITY/RELEASE_DIFF → 20; default 10). cross_doc 0% → 37.5%.
  - **commit dc6116e** — eval: refresh ground truth. Surveyed 18 questions × ~50 expected req_ids; 2 misses, both struck section-headers correctly absent (`_LTEB13NAC_23513` IMS REGISTRATION REQUIREMENTS; `_LTEB13NAC_23532` IMS REG THROTTLING ACROSS TRANSITIONS). Replaced with live siblings (`_6444` retry algorithm, `_6455` live throttling-across-transitions); both struck reqs added to integration test's `STRUCK_REQ_IDS` for positive must-be-absent assertion. trace_02 50% → 100%; traceability 50% → 66.7%.
  - Per-category final: feature_level 100%, single_doc 79%, traceability 66.7%, standards_comparison 50%, cross_doc 37.5%. 4 remaining failures (cross_03/04, standards_01, trace_03) are all the same shape — concept queries where the right req exists but ranks below richer leaf chunks. Identified as a retrieval-quality problem (BM25 hybrid candidate); deferred to next session.
- 2026-05-02 (evening) BM25 hybrid retrieval shipped (commit 3257209). New `core/src/query/bm25_index.py` — telecom-aware tokenizer (preserves `vz_req_lteat_45` / `24.301` / `rel-9` as single tokens), `BM25Index` wrapping `rank_bm25.BM25Okapi` with own (id,text,metadata) cache, `rrf_fuse(*lists, weights, k=60)` for weighted Reciprocal Rank Fusion. `VectorStoreProvider` protocol gains `get_all() -> QueryResult` (ChromaDBStore implements). `RAGRetriever` accepts optional `bm25_index` and per-call `bm25_weight`; pure-dense path preserved when `None` or `weight=0`. `QueryPipeline` builds the index at init via `BM25Index.from_store()` and picks `bm25_weight` from new `_TYPE_BM25_WEIGHT` map keyed by `intent.query_type`. Empirical tuning on env_vzw: STANDARDS_COMPARISON / TRACEABILITY / SINGLE_DOC = 0.5; CROSS_DOC / FEATURE_LEVEL = 0.0 (parent chunks too thin to compete with BM25-favored leaves). 23 new tests; standards_comparison +33pp accuracy. Captured as D-040.
- 2026-05-02 Eval ground-truth refresh + classifier fix (commits 0799150 + 85163f2). First pass replaced 3 wrong/voided reqs in expected_req_ids: trace_03's `_7754` (cause code 42, not 22) → `_7795 + _7761` (real cause-code-22 reqs); cross_03's `_LTESMS_30259` (chunk text "VOID") → `_LTESMS_29575`; cross_04's `_LTEDATARETRY_23850` (about ATTACH retry, not detach) → `_LTEDATARETRY_7760`. Plus added "mention" / "mentions" / "mentioned " as TRACEABILITY classifier triggers (before the cross-doc check) so concept-lookup queries route correctly and BM25 fires; trace_03 0%→50%. Second pass: user-curated broadening of every question's expected_req_ids — most lists grew from 1-2 to 4-8 reqs reflecting actual related-content depth. Single whitespace typo on `_LTEDATARETRY_7759` corrected.
- 2026-05-02 Vectorstore: parent-chunk subsection augmentation, opt-in (commit 2e01bcb). New config flags `include_children_titles` (default **False**), `children_titles_body_threshold` (300), `max_children_titles` (3). When enabled and parent body is below threshold, appends `[Subsections: t1; t2; (+N more)]` line to the chunk text. Default off because empirical tuning on OA corpus showed +8pp single_doc / -10..14pp cross_doc tradeoff (augmented parents displace their children from top-k; breadth queries actually want the children). 89% of OA parents are body<50 chars (heading-only), so the body-thinness gate doesn't selectively help on this corpus. Feature stays available for corpora with rich-bodied parents or lookup-heavy question mixes. 8 new tests.
- 2026-05-02 Drift-check dev-full (commit 60815e4): 9 drifts resolved, all direction [b] (MODULE.md → match code) reflecting D-035..D-040 from the prior session — parser ParseStats fields, cascade/revhist invariants, definitions dual-layout, extraction strikethrough per-block-type, profiler revhist field + key choice, query entity-priority + type-aware top_k key choices.
- 2026-05-02 README: 102-line Tuning section (commit d6dca4d) covering all 4 layers — profile JSON, vectorstore config, query pipeline Python constants, env vars / CLI flags. Each layer documents location, when-to-tune, re-run cost; final symptom→knob lookup table. Project Structure section removed in follow-up (commit d7dae4c — duplicated MAP.md).
- 2026-05-02 (evening) **A4 eval final** (provisional, later superseded — see 2026-05-03 entry): avg_overall=88.9% / avg_accuracy=80.1% (qwen3-235b-a22b via OpenRouter). Per-category: feature_level 100%, single_doc 80.6%, traceability 83.3%, standards_comparison 83.3%, cross_doc 55.0%.
- 2026-05-03 **Baseline correction — A4 was not reproducible**. User-driven full-pipeline rebuild (A6: `python -m core.src.pipeline.run_cli --env-dir ~/work/env_vzw --llm-provider openai-compatible --model qwen/qwen3-235b-a22b --standards-source huggingface`) gave 86.7% / 60.3%, ~20pp accuracy below A4. Three-run variance experiment on the same MiniLM vectorstore (A8) gave overall=87.2-88.9% / acc=62.1-68.8% (mean 88.3% / 64.3%, stdev ~3.8pp). A4's 80.1% is ~4σ above this distribution — not LLM stochasticity. **Diagnosis**: A4 was measured via `--start eval --end eval` against an intermediate graph state from earlier in the session, which had ~3500 more `maps_to` edges than today's freshly-built graph (n=1290 e=9257 vs A4-era n=1271 e=12673). Source of the gap is non-deterministic taxonomy LLM output — different runs produce different feature mappings → different graph topology → different retrieval-friendly candidate sets. **The honest deterministic baseline on the current code with a fresh full-pipeline build is 88.3% overall / 64.3% accuracy** (3-run mean). A4's 80.1% remains aspirational — recoverable only by pinning taxonomy output (LLM temperature=0 + caching) or by improving retrieval to be less graph-topology-dependent.
- 2026-05-03 Embedding model swap experiment (A7): rebuilt vectorstore with `qwen3-embedding:4b` via Ollama (instead of MiniLM). Required raising the per-request Ollama timeout from 60s → 300s default in `make_embedder` (4B-class model on CPU exceeds 60s on ~8000-char chunks; user's first attempt failed at batch 11/13 with `VEC-E001: Build failed: timed out`). Also added `NORA_OLLAMA_TIMEOUT_S` env-var override. Vectorstore rebuild took 77 min (vs MiniLM's 70s — 65× slower); vectors are 6.7× larger (2560-dim vs 384-dim). A7 result: 87.2% / 61.9%, no measurable lift over MiniLM at A6/A8 baseline. Per-category split was informative: single_doc +20.6pp, cross_doc -13.4pp, others unchanged. Same parent-vs-leaf displacement effect as the chunk-augmentation experiment — qwen3-embedding ranks token-rich chunks higher (helps lookup, hurts breadth). Net cost of 65× embed time + 6.7× storage for ~zero net gain. **Reverted to MiniLM as the default embedding** for env_vzw via the A8 rebuild that produced the 64.3% mean baseline.
- 2026-05-03 (evening) **Acronym lookup chain shipped** (D-043, commits 843df8d + 4aa9123). Three-layer fix triggered by a user-flagged hallucination on the Test page ("What is SDM?" → invented "SIMOTA Device Management" because the real glossary chunk didn't make top-K): (a) parser recovers misclassified table-header rows so `definitions_map` keeps acronyms whose row landed in `tbl.headers` after a stray `|---|` divider — VZW LTEOTADM goes 18 → 19 entries; (b) chunk_builder emits one short chunk per `(acronym, expansion)` entry (`doc_type=glossary_entry`); (c) `RAGRetriever` builds an in-memory acronym index at init, detects "What is X / Define X / Meaning of X / Expand acronym X" patterns, and prepends the matching glossary chunk to retrieval (after rerank, with chunk_id dedup). 13 new tests. Detailed design in `core/src/query/RETRIEVAL.md`.
- 2026-05-03 (evening) **Test page UX + web infra polish** (commit 843df8d). Test page splits citations into "Cited by LLM" (extracted from answer text) vs "Returned by RAG" (full Stage-4 retrieval); each is click-to-expand for the chunk fragment text. Web `routes/query.py` now reads from `<env_dir>/out/{graph,vectorstore}` (D-022) and constructs the LLM provider from `environments/<env>.json` (matches eval), instead of legacy hardcoded paths + Ollama. QueryPipeline cached on `app.state` so the heavy components (graph, embedder, BM25, reranker) build once per process instead of per request. Configurable DB paths (jobs / metrics / feedback) via a 4-layer chain: CLI flag > env var (`NORA_{JOBS,METRICS,FEEDBACK}_DB`) > `config/env.json` > `<env_dir>/state/<default>.db`. Pipeline CLI gains `$ENV_DIR` fallback parallel to the web UI. 13 new tests + smoke. Design impact summarized in retrieval doc; deeper structure changes out of scope here.
- 2026-05-04 **Unified LLM config (D-044) + RAG-only pipeline mode (D-045)** (commit 68d9961). New `config/llm.json` is the single canonical home for LLM + embedding settings (provider, model, timeout, base URL, API key, embedding provider/model, ollama url/timeout, plus `skip_taxonomy` / `skip_graph` toggles). Per-field 3-tier resolution: CLI flag > env var > `config/llm.json` > deprecated env-config back-compat fallback. New `--skip-graph` / `--rag-only` flags + `NORA_SKIP_GRAPH` / `NORA_RAG_ONLY` env vars + `EnvironmentConfig.skip_graph` field. Query path tolerates missing graph: new `build_stub_graph_from_store` helper builds a minimal MNO/Release/Plan-only graph from vectorstore metadata at runtime; pipeline runs with `_bypass_graph=True` so Stage 3 emits an empty CandidateSet and Stage 4 falls back to MNO/release metadata filtering. Eval stage applies the same fallback. 15 new tests (LLMConfigFile, 3-tier resolution, NORA_RAG_ONLY-implies-both, stub graph topology + end-to-end). Captured as ADRs D-044 (unified config) + D-045 (RAG-only mode + stub-graph fallback).
- 2026-05-04 **Web/CLI infrastructure polish** (commits cce52df + babd9f0 + 663102d). README config sections collapsed into a single 3-tier table (env / web db / web / llm / pipeline rows × CLI / env var / config-file columns; -61 +31 lines). Pipeline CLI honors `$ENV_DIR` fallback parallel to the web UI. Bug fix: `_build_pipeline` in `routes/query.py` was hardcoded to `SentenceTransformerEmbedder`, which broke RAG-only mode with Ollama-built vectorstores ("Repo id must use alphanumeric chars" on `qwen3-embedding:4b`); rerouted through `make_embedder(vs_config)` factory so it dispatches by `embedding_provider`. Verified `OllamaEmbedder.dimension=2560` against the env_vzw qwen3 store.
- 2026-05-04 **Git history hygiene** — stripped `Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>` trailer from 85 commits via `git rebase --exec`, force-pushed to `origin/main`. SHA reference sweep in `STATUS.md` + `DECISIONS.md` (45 token replacements; new commit on top: 32a7094). Three guardrails to prevent regression: (a) per-project memory entry under `~/.claude/.../memory/feedback_no_claude_coauthor.md`; (b) local `.git/hooks/commit-msg` that strips the trailer plus "🤖 Generated with Claude Code" markers; (c) reviewer eyes on diffs. Backup refs `refs/backup/{main,origin-main}-before-strip` retained locally; safe to delete after the work-laptop is re-synced. Caught and corrected one false-start where `--no-edit-on-empty` (not a real git flag) silently no-op'd the rebase.
- 2026-05-04 `/regen-map` (commit 1c674cf) — refreshed Structure section in all 16 MODULE.md files + `docs/compact/MAP.md` (284 tree rows, 146 with descriptions). Drift folded into curated **Public surface** sections: `web` gained `EnvJsonConfig`, `FeedbackStore`, DB-path overrides, `feedback_db_path()`, `DEFAULT_ENV_JSON_PATH`, playground router; `query` hoisted `QueryRewriter` + `Reranker` Protocols as durable contracts. Self-check confirmed every byte outside `<!-- BEGIN/END:STRUCTURE -->` markers is byte-identical to HEAD.
- 2026-05-05 **Retrieval quality Step 1 + Step 2 shipped** (commit 664e648 + uncommitted web wiring).
  - **Step 1 — document-rooted hierarchy paths**: `chunk_builder` prepends `plan_name` (or `plan_id` fallback) as the root of `[Path: ...]` and stores the full Document > Section > Subsection chain in chunk metadata (`hierarchy_path` as `list[str]`). `context_builder._enrich_chunk` prefers chunk metadata path over graph node path; old vectorstores degrade gracefully via the graph fallback. 11 new tests (`test_chunk_builder_hierarchy.py`); existing `test_vectorstore.py::test_chunk_text_has_hierarchy_path` updated for the new format.
  - **Step 2 — relevance threshold + "not found" response**: `QueryPipeline` accepts `max_distance_threshold: float | None`; Stage 4.5 drops chunks with cosine distance above the threshold, and returns `_NOT_FOUND_ANSWER` (skipping context assembly + LLM synthesis) when every chunk is filtered. Per-intent override map `_TYPE_MAX_DISTANCE` left empty pending intent classification (Step 4). 13 new tests.
  - **Threshold calibrated to 0.5** on env_vzw corpus + qwen3-embedding:4b-q8_0: relevant queries score 0.20-0.41, off-topic 0.74-0.77 (gap 0.33). Wired into the web pipeline build with `NORA_MAX_DISTANCE_THRESHOLD` env-var runtime override (uncommitted; this commit).
- 2026-05-05 **vectorstore_cli ↔ config/llm.json wiring** (committed in 664e648). `_build_config` now consults `config/llm.json` for `embedding_provider` / `embedding_model` via the existing `resolve_embedding_*` helpers. Precedence: CLI flag > `NORA_EMBEDDING_*` env > config-file tier (Option A: `--config <path>` if supplied, else `config/llm.json`) > defaults — `--config` replaces `config/llm.json` at the same tier rather than stacking on top, so frozen experiment configs aren't silently overridden by the project default. Two pre-existing `test_env_config.py` bugs fixed (tests didn't isolate from on-disk `config/llm.json`).
- 2026-05-05 `config/llm.json` defaults switched to **ollama + qwen3-embedding:4b-q8_0** with `skip_taxonomy: true` / `skip_graph: true`. Ollama model `qwen3-embedding:4b-q8_0` (4.3 GB) pulled on dev PC. Vectorstore rebuilt against this model (`/home/mohan/work/env_vzw/out/vectorstore`). Other team members can override via `NORA_EMBEDDING_*` env vars.

## In progress

- **Retrieval improvements plan — Steps 3-5 remaining**:
  - Step 3: hierarchy-based grouping + gap-based auto-commit + disambiguation UX
  - Step 4: query intent classification (Lookup / Fact / Comparison / Survey / Summarize / Procedural) — populates `_TYPE_MAX_DISTANCE` and per-intent retrieval/synthesis rules
  - Step 5: per-sentence citation polish layer
  - **Deferred**: Comparison-intent expansion needs multi-release / multi-MNO ingestion before it can be tested.

## Next

- **Browser smoke-test of Step 1 + Step 2** before starting Step 3 — start `python -m core.src.web.app --env-dir /home/mohan/work/env_vzw --port 8000`, confirm relevant queries answer normally and off-topic queries return the not-found message.
- **Re-calibrate `max_distance_threshold` if/when the embedding model changes** (currently 0.5 for qwen3-embedding:4b-q8_0). Helper script `tools/threshold_sweep.py`-equivalent recipe lives in the conversation transcript; could be promoted to `core/scripts/` if used regularly.
- **RAG-only A/B against full-pipeline baseline** — measure RAG-only retrieval (skip taxonomy + graph) on the 18-Q eval set vs the deterministic A8 baseline (88.3% / 64.3%). Hypothesis: graph scoping is dead weight for concept-shaped queries (cross_doc / standards_comparison) and helps lookup-shaped queries (single_doc / traceability). Per-category split should reveal which.
- **Domain-tuned cross-encoder** — `ms-marco-MiniLM-L6-v2` was net-zero (slight regression) on technical spec queries (A11 result: 86.4% / 62.3%). Code is plumbed; swap to a cross-encoder trained on telecom/legal text, or train one on the eval set's positives. ~5pp accuracy lift would be expected if the OOD-vs-corpus gap is the right diagnosis.
- **Retrieval-quality next-level** — the remaining missing reqs are concept-shaped queries where the right req exists in corpus + graph + vector store but doesn't rank in top-k. Already shipped: BM25 hybrid (D-041), per-type top_k (D-040), cross-encoder reranker (off by default after A11), query rewriting (per-type-gated), glossary lookup chain (D-043). Next candidates: learned BM25 weights per query type, parent-chunk augmentation strategies that don't displace leaves.
- **Continue audit-driven verification rounds** with `parse_audit` CSVs at `<env_dir>/reports/audit/<doc>_audit.csv` — focus on MEDIUM rows we haven't sampled yet (deeply nested LTEB13NAC depths 7-9; mid-doc LTESMS), grow `GROUND_TRUTH_PAIRS` in the integration test as more pairs are verified.
- Replicate baseline on work-laptop (gemma4:e4b on RTX A4600) against the same 5 OA PDFs at `<env_dir>/input/VZW/OA-baseline/` for apples-to-apples model comparison. **Note**: requires removing any stray non-OA docs from the work-laptop input dir first.
- Investigate `xp=1` cross-plan refs — parser only catches cross-plan refs through embedded `VZ_REQ_<PLAN>_<NUM>` patterns; misses prose-style references. Fix: profile-driven plan-name registry + prose detection, OR resolver-level fuzzy matching against `{plan_id: [plan_name, ...]}`.
- Harden `--llm-provider` flag so when explicit (not defaulted), `require_real=True` is forced — missing env vars hard-fail with a clear message instead of silently falling back to mock.
- Web UI smoke test — start `python -m core.src.web.app`, exercise correction editor / pipeline form / metrics dashboard against the new `<env_dir>` layout (templates were updated but not user-tested).
- Optional `/drift-check requirements` pass to confirm FR-28..FR-30 are actually exercised by code.
- `/drift-check design` (later) to surface any code capabilities still lacking an owning FR / NFR.
- Revisit triggers on the three deferred items when they fire (see `core/src/web/MODULE.md` and `core/src/pipeline/MODULE.md` Deferred sections).

## Flags

- 2026-04-27 [requirements] Latency / throughput / memory NFRs not yet set; defer until first work-laptop full-pipeline run produces real telemetry, then promote thresholds to NFR entries in `requirements.md`.
- 2026-04-27 [requirements] KG sharding / persistence: networkx single-process graph fits v1 (~1k nodes, ~12k edges) but needs re-evaluation when 2nd MNO corpus is added. Revisit at start of post-v1 multi-MNO ingestion.
- 2026-04-28 [development] Parser dual-anchor (D-027) caused 5.6-pt eval accuracy regression (60.2→54.6) on the 18-Q OA eval set despite structural numbers moving in the right direction. Citation drop 100→94.4% is the smoking gun → retrieval is pulling new table-anchored thin chunks as distractors. Eval set was authored against the pre-fix corpus and doesn't ask about any of the 274 new table-anchored req IDs. Mitigation TBD (see In Progress).
- 2026-05-01 [development] One LOW-confidence row remains in OA parse audit: `_LTESMS_30297` at depth 11 (`1.5.1.1.3.35.1.1.3.1.2`). Source document has two consecutive section numbers MERGED ("1.5.1.1.3.3" + "5.1.1.3.1.2") — a corpus defect, not a parser bug. Correct section is `1.5.1.1.3.3` per user. Resolution: corrections-workflow override on this single req_id; defer until corrections-workflow polish.
- 2026-05-01 [development] `enable_table_anchored_extraction` defaults to True (backward-compat with D-027). For paragraph-only-requirement MNOs (Verizon OA), the flag should be set to False via corrections file or auto-detected. New corpora may need a corrections-workflow profile edit to disable; consider auto-detection (if >90% of table-anchored ids are duplicates of paragraph-anchored ones, flip the flag) as a future enhancement.
- 2026-05-01 [development] D-034 rule 1 (req-id placement) is hardcoded trailing-marker in the parser today. Per the decision rationale, this is a corpus property and should be detected at profile stage: add `RequirementIdPattern.placement: str = "trailing" | "leading"`, have profiler analyze adjacency between small-font req_id blocks and heading-shaped blocks, and have parser branch on the flag. Default "trailing" preserves OA behavior. New corpora with leading-marker req_ids will mis-parse until this lands; flag surfacing the next time a non-VZW corpus is encountered.
- 2026-05-02 [development] A4 eval citation_quality dropped 94.4% → 88.9% vs A3 (gemma → qwen synthesizer change, not a structural issue). Standards_integration also dropped 88.9% → 83.3%. Both are LLM-formatting differences; don't represent real regressions but the eval rubric's strictness penalizes them. Consider tuning the synthesizer prompt for citation/standards-section formatting consistency, or relaxing the rubric for stylistic-but-correct answers.
- 2026-05-02 [development] Eval ground-truth review needed periodically as parser improves. Today caught 2 struck-reqs that the parser correctly drops but were still in expected_req_ids; replaced with live siblings. Recommend a "ground-truth audit" pass any time the strike/cascade/revhist logic changes — survey expected_req_ids against current parsed tree, flag absent ones, classify as (a) replace with live sibling, (b) add to STRUCK_REQ_IDS, or (c) bug.
- 2026-05-02 [development] Chunk augmentation (`include_children_titles`) shipped default-off. The OA-corpus tradeoff is fundamental — 89% of parents are heading-only so augmentation makes them dominant in cross-doc retrieval, displacing the children that breadth queries want. Body-thinness gate doesn't selectively help. Re-evaluate the toggle on corpora with rich-bodied parents (TMO/AT&T) or lookup-heavy question mixes; the feature is implemented + tested behind the flag.
- 2026-05-03 [development] **Taxonomy LLM is the source of pipeline non-determinism**. A8 variance experiment showed 3-run accuracy spread of 6.7pp (62.1-68.8%) on the same vectorstore — that's pure synthesizer LLM noise. But A4 vs A6 had a 20pp gap that ISN'T noise (~4σ), explained by graph-topology shifts (~3500 fewer `maps_to` edges in A6) that trace back to taxonomy LLM producing different feature mappings each run. Hardening: set taxonomy LLM temperature=0 and/or cache its output by content hash so the graph becomes reproducible. Current behavior makes baseline numbers ambiguous — every comparison needs at least 3 runs to be meaningful.
- 2026-05-05 [development] `config/llm.json` now ships with non-empty defaults (ollama + qwen3-embedding:4b-q8_0, skip_taxonomy/graph=true) instead of the previous all-empty fall-through state. Team members pulling main inherit these. Override via `NORA_EMBEDDING_*` env vars or a custom `--config <path>`. Revert the file to empty defaults if this becomes confusing for new contributors.
- 2026-05-05 [development] `max_distance_threshold` default of 0.5 is calibrated specifically to qwen3-embedding:4b-q8_0 on the OA corpus. Other embedding models will need a re-sweep — relevant/off-topic distance distributions shift per model. Track this when revisiting embedding choice.
- 2026-05-05 [design] MODULE.md drift deferred from close-session: D-046 (chunk metadata `hierarchy_path` field) and D-047 (`max_distance_threshold` on `QueryPipeline` + Stage 4.5 invariant) are not yet reflected in `core/src/vectorstore/MODULE.md` and `core/src/query/MODULE.md` Public surface / Invariants. Resolve via `/drift-check dev-module vectorstore` and `/drift-check dev-module query` next session.
